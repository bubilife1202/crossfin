{
  "name": "@crossfin/docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "vitepress build && npx wrangler pages deploy .vitepress/dist --project-name crossfin-docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
